# Desktop Organization — `_Organized/`

Generated by **Hermes** (scheduled cron job) on **2026-06-25**.

This directory contains the consolidated, categorized output of the previous
`~/Desktop/` layout. **No files were deleted** — every original item lives
somewhere under this tree.

## Start here

| File | What it is |
|---|---|
| **`CONSOLIDATION_REPORT.md`** | Human-readable writeup of what was done, where it went, and how it was deployed |
| **`MANIFEST.csv`** | Full file inventory (195k+ rows): path, category, size, mtime, extension |
| **`consolidate.sh`** | The bash script that performed all the moves (idempotent, re-runnable) |
| **`build_report.py`** | The Python script that scanned the tree and produced MANIFEST.csv + the report |

## Layout

```
_Organized/
├── Documents/      CV, job applications, downloaded HTML
├── Media/
│   ├── Audio/      WAV, MP3, AIF
│   ├── Video/      MOV, MP4 (screen recordings)
│   └── Images/     PNG, JPG, SVG, PXD, AVIF, TIFF
├── Code/           JS, PHP, JSON config
├── Archives/       ZIP, DMG
├── Other/          Unclassified (filename quirks)
└── _Projects/      29 pre-existing project directories, preserved as-is
```

## What's in this git repo

Only this README, the report, the manifest, and the build scripts are
committed and (when you choose to push) sent to GitHub. **All binary content
(media, archives, project bundles) is excluded by `.gitignore`.**

See `CONSOLIDATION_REPORT.md` §9 for GitHub deployment instructions and the
manual `gh repo create` command to run.